// @flow
/* eslint-disable react/jsx-filename-extension */
import React, { type ElementProps } from 'react';
import { Platform } from 'react-primitives';


import { Box, Text } from '../../../../atoms/components';

let Input;

// TODO: App support
/* if (Platform.OS === 'ios' || Platform.OS === 'android') {
  const { TextInput } = require('react-native-web');

  Input = props => <TextInput {...props} />;
} else */if (Platform.OS === 'sketch') {
  Input = ({
    placeholder, value, children, ...props
  }: ElementProps<typeof Box> & {
    value?: string,
    placeholder?: string,
  }) => (
    <Text {...props}>
      {placeholder || value}
    </Text>
  );
} else if (Platform.OS === 'web') {
  Input = ({ value, children, ...props }: ElementProps<typeof Box> & {
    value?: string,
  }) => (
    <Box as="input" {...props} />
  );
}

export default Input;
// export { default } from './Input.sketch';
